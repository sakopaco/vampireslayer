nstall Linux version with commands below:

sudo add-apt-repository ppa:amaurycarvalho/nmsxtiles
sudo apt-get update
sudo apt-get install nmsxtiles

Use it with command:

nMSXtiles


130 a 143 sin el and y usando CALL+RET en vez de JP